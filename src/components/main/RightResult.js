import React from 'react';
import { Box, Grid } from '@material-ui/core';

const RightResult = () => {
    return (
        <Grid md={8} item >
            aa
        </Grid>
    );
};

export default RightResult;