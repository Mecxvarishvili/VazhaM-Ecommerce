import { Box } from '@material-ui/core';
import React from 'react';

const Footer = () => {
    return (
        <Box component="footer">
            footer
        </Box>
    );
};

export default Footer;